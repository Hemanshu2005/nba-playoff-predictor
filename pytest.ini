[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    unit: isolated unit tests for a single function
    integration: tests that combine multiple modules
    slow: tests that run many simulations (marked to skip in fast CI runs)
